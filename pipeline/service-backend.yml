include:
  - pipeline/templates.yml

service-backend:lint:
  extends: .rust-template
  stage: lint
  needs:
    - job: cargo:cache
      optional: true
  script:
    - rustup component add rustfmt
    - cargo fmt -p service-app -- --check
